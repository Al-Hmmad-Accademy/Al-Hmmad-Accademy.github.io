 'use client';import {useEffect,useState} from 'react';export default function Books(){const [books,setBooks]=useState([]);useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_URL||'https://steps-accademy-backend-production.up.railway.app/api'}/content/books`).then(r=>r.json()).then(x=>setBooks(x.books||[])).catch(()=>{})},[]);const subjects=[...new Set(books.map(b=>b.subject))];return <><section className="page-hero"><div className="container"><div className="eyebrow">Digital Library</div><h1 style={{fontSize:'clamp(45px,7vw,80px)',marginTop:18}}>Books by subject.</h1><p className="lead">Read online or download approved PDF resources.</p></div></section><section className="section"><div className="container">{subjects.length?<div className="grid cards">{subjects.map(s=><div className="card feature" key={s}><span className="tag">{s}</span><h3 style={{marginTop:16}}>{books.filter(b=>b.subject===s).length} resources</h3><div className="list">{books.filter(b=>b.subject===s).map(b=><a className="list-row" href={b.url} target="_blank" rel="noreferrer" key={b._id}><b>{b.title}</b><span>PDF ↗</span></a>)}</div></div>)}</div>:<div className="card feature"><h3>Library is being prepared.</h3><p className="lead">Approved books will appear here.</p></div>}</div></section></>}
