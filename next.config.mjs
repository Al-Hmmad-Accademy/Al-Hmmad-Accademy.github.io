/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Stepaccademy',
  assetPrefix: '/Stepaccademy/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;